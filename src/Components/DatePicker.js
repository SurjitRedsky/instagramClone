import React, { useState } from 'react'

const DatePicker = () => {

    const [dayValue, setDayValue] = useState(second)

    return (
        <>
            <select>
                <option value={""}> {""}</option>

            </select>
        </>
    )
}

export default DatePicker